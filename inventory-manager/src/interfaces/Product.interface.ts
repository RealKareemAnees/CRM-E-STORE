export interface ProductInterface {
  title: string;
  description: string;
  newPrice: number;
}

export interface ProductWithIDInterface {
  _id: string;
  title: string;
  description: string;
  newPrice: number;
}

export interface productID {
  _id: string;
}
